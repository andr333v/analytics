define(["require", "exports"], function(require, exports) {
    var Title = (function () {
        function Title(text) {
            this.text = text;
        }
        return Title;
    })();
    exports.Title = Title;
});
